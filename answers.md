Q1) 
HTML: builds the structure of the page
CSS: controls the design and layout 
JavaScript: adds interactivity and logic



Q2)
HTTP request: is sent from the client to the server asking for data or an action. 
HTTP respond: the result that the server will send it back


Q3)
GET: is used to get the data from the server
POST: is used to send new data to the server


Q4)
HTTP status code : it is a code number returned by the server to describe the result of a request
404: not found , 200 : success , 400 : bad request


Q5)
JSON : its an format data used to exchanged data
API used it because its simple and support many languages


Q6)
we used API so we can communicate with another system
EX: weather app , payment gateways


Q7)
REST is a style for designing APIs
It uses HTTP methods, stateless requests, and uses clear resource


Q8) 
The browser will send an HTTP request to the server. Then the server will responds with data , then the browser renders the page


Q9)
Authentication: verifies who the user is
Authorization: decides what the user is allowed to access


